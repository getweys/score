# Contact form email settings
# For local dev: copy to `.env` or `.env.local` and fill in the values below.
# For production: set the same variables in your hosting panel.
# Ask IT / hosting for SMTP details for info@score.com.pk.

SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
CONTACT_FORM_TO=arhamta123@gmail.com
CONTACT_FORM_FROM=
